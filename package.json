{
	"private": true,
	"scripts": {
		"clean": "rm -rf dist",
		"build": "mkdir -p dist && cp site/index.html dist",
		"all": "npm run clean && npm run build",
		"watch": "nodemon --exec 'gulp watched'"
	},
	"devDependencies": {},
	"dependencies": {}
}
